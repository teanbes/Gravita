using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using TMPro;
using UnityEngine;
using UnityEngine.InputSystem;
using UnityEngine.UI;

[System.Serializable]
public class SkinItem : MonoBehaviour
{
    //[SerializeField] GameObject playerSkinButton;
    //[SerializeField] private Color color;
    //[SerializeField] private TextMeshProUGUI priceText;


    //private void Start()
    //{
    //    color = playerSkinButton.transform.GetChild(0).GetComponent<Image>().color;
    //    priceText.text = playerSkinButton.transform.GetChild(1).GetComponent<TextMeshProUGUI>().text;
    //}

    //public void UpdatePrice(string newPrice)
    //{
    //    priceText.text = newPrice;
    //}
}
